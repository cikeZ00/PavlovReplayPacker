# Pavlov Replay Packer
This module is made for packaging HTTP replay chunks from LocalPavTV into full UE ``.replay`` binaries.

## Usage
Place all files from a given replay inside ``replay_chunks``.
Run ``yarn``
Run ``node .\index.js``
